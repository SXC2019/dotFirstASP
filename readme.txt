Install git
For the first time, clone this repo using command: git clone https://github.com/SXC2019/dotFirstASP.git
After every class for the update, use the command: git pull origin main
If not main then try master instead of main.
Set the directory to the cloned folder first before pulling the updates.